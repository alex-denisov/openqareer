/**
 * Потолок одной ступени очереди провайдеров.
 *
 * Решение владельца 2026-09-03 (B180): ступень, которая думает дольше
 * тридцати секунд, кандидату уже не помогает — за её спиной стоит очередь из
 * пяти моделей, и лучше отдать ход следующей, чем ждать. Клиентские потолки
 * стояли в 60–90 секунд, поэтому одна медленная ступень съедала весь ход.
 *
 * Ступень тратит эти тридцать секунд один раз: повтор внутри клиента
 * удваивал бы бюджет молча. Повтор — это и есть следующая ступень очереди.
 */
export const PROVIDER_STAGE_TIMEOUT_MS = 30_000;

/** Повторов внутри ступени нет: за повтор отвечает очередь. */
export const PROVIDER_STAGE_MAX_RETRIES = 0;
