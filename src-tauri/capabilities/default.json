{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for OpenQareer desktop shell",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute",
    "process:default"
  ]
}
